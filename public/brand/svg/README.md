# SVG brand files

All five SVGs are in use. PNG fallbacks live in `../logo-primary-560.png` and `../logo-mark-192.png`.

PWA home screen uses root `icon-192.png` / `icon-512.png` (PNG required by manifest).
